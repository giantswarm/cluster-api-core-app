name: cluster-api-core

images:
  controller:
    name: k8s-staging-cluster-api/cluster-api-controller
    tag: nightly_master_20210803

Installation:
  V1:
    Registry:
      Domain: gcr.io

featuregates:
  machinepool: true
  clusterresourceset: true

ports:
  healthz: 9440
  metrics: 8080
  webhook: 9443

project:
  branch: "[[ .Branch ]]"
  commit: "[[ .SHA ]]"

watchfilter: capi
